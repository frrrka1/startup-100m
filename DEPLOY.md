# Vercel deploy

1. Vercel → Add New → Project.
2. Import `frrrka1/startup-100m`.
3. Framework preset: Other.
4. Build command: empty.
5. Output directory: empty / root.
6. Deploy.

After deploy, replace messenger placeholders in `index.html` with the real contact number and redeploy.
